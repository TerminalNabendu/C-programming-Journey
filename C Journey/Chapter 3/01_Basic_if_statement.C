#include <stdio.h>

int main() {
    int age = 20;

    // Check if age is greater than 18
    if (age > 18) {
        printf("You can drive\n"); // Executes if condition is true
    }

    return 0;
}